{
  "name": "cycle-circular",
  "version": "0.0.1",
  "description": "Cycle.js helper for circular dependencies.",
  "author": "whitecolor",
  "license": "ISC",
  "main": "lib/index.js",
  "keywords": [
    "rx",
    "dx",
    "cyclejs"
  ],
  "scripts": {
    "clean": "rimraf lib",
    "build": "babel src -d lib",
    "build:watch": "babel src -w -d lib",
    "test": "mocha --compilers js:babel-register test/index.js",
    "test:watch": "npm run test -- --watch",
    "prepublish": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@cycle/rx-adapter": "^1.0.3",
    "@cycle/rx-run": "^7.0.0-rc4",
    "@cycle/rxjs-adapter": "^1.0.5",
    "@cycle/rxjs-run": "^1.0.3",
    "@cycle/xstream-adapter": "^1.0.3",
    "@cycle/xstream-run": "^1.0.1",
    "assert": "^1.3.0",
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "mocha": "^2.4.5",
    "rx": "^4.1.0",
    "rxjs": "^5.0.0-beta.7",
    "xstream": "^2.2.1"
  }
}
